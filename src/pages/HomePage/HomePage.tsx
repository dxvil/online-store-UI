import React from "react";
import {Header} from "../../containers/Header/Header";

const HomePage = () => {
	return (
		<div>
			{/* header: menu, logo, basket */}
			<Header />
			{/* intro: some info, products */}
			{/* footer: links, contact */}
		</div>
	);
};

export default HomePage;